# Dependencies
node_modules/

# Build intermediates
.vite-tmp/
*.tsbuildinfo

# dist/ is fully generated by `pnpm build` — never commit it
dist/

# Release zip — generated by `pnpm build`
livecam-dist.zip

# Recorded output (don't commit captured video)
*.mov
*.mp4
*.avi
*.webm

# Editor / agent local state
.claude/
discarded/

# OS noise
.DS_Store
Thumbs.db
desktop.ini
